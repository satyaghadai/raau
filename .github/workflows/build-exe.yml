name: Build Windows Executable

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pyinstaller pywin32 wmi psutil reportlab

    - name: Build EXE with PyInstaller
      run: |
        pyinstaller --noconfirm --onedir --windowed --name "RAAU_Assessment_Utility" --hidden-import=win32com --hidden-import=wmi --hidden-import=psutil --collect-all reportlab --collect-all psutil main.py

    - name: Upload Windows Executable Artifact
      uses: actions/upload-artifact@v4
      with:
        name: RAAU_Windows_EXE
        path: dist/RAAU_Assessment_Utility/
